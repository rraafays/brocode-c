#include <stdio.h>

int main(int argc, char *argv[])
{
  const float PI = 3.14159; // constants are fixed values that cannot be altered by the program during its execution

  printf("pi: %.5f", PI);

  return 0;
}
